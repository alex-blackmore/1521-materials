#include <stdio.h>
#include <stdbool.h>
#include <pthread.h>


void *thread_run(void *data) {
    // cast

    // print str (data)
}

int main(void) {
    // create thread

    // print str

    // wait for thread to finish (not really needed?)
}